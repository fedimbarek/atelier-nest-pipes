import { GenerateurRepository } from './generateur-repository';

describe('GenerateurRepository', () => {
  it('should be defined', () => {
    expect(new GenerateurRepository()).toBeDefined();
  });
});
